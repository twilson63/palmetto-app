var h = require('virtual-dom/h');

home.ref = '/home';
home.render = render;
module.exports = home;

function home(state, ee) {
  ee.on('/home', function() {
    state.set('ref', '/home');
  });
}

function render(state) {
  return h('h1', ['Home']);
}